# Legacy Style Copies

This directory is kept only for compatibility with older local tests.

Do not use it for generation. Prompt to Canvas must choose styles from root `CATALOG.md` and read
only root `templates/<slug>/design.md`, which are synced from
`zarazhangrui/beautiful-feishu-whiteboard`.
